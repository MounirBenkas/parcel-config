{
  "name": "tuto-parcel",
  "version": "1.0.0",
  "description": "Configuration Parcel",
  "main": "index.js",
  "dependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "devDependencies": {
    "sass": "^1.32.8"
  },
  "scripts": {
    "dev": "parcel index.html -p 3000 --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build index.html --no-source-maps",
    "build": "npm run clean && npm run build:parcel"
  },
  "author": "Mounir B",
  "license": "ISC"
}
